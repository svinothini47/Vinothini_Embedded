#include <stdio.h>

int main() {
    for (int i=0;i<=15;i++)
    {
        if(i==9)
        {
            continue ;
        }
        if(i==13)
        {
            break;
        }
        printf("%d\n",i);
    }
    return 0;
    
}
